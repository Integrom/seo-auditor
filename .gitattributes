# Нормализуем переводы строк: в репозитории всегда LF
* text=auto eol=lf

# Shell-скрипты обязательно LF — иначе не запустятся на сервере
*.sh text eol=lf

# Бинарные файлы не трогаем
*.png binary
*.jpg binary
*.pdf binary
*.woff binary
*.woff2 binary
